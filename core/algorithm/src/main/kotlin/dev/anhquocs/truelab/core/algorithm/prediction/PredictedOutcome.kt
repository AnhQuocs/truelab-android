package dev.anhquocs.truelab.core.algorithm.prediction

/**
 * Enum biểu diễn 3 kết quả thi đấu có thể xảy ra trong một trận đấu.
 */
enum class PredictedOutcome {
    HOME_WIN,
    DRAW,
    AWAY_WIN
}
